var React = require( 'react' );

var List = React.createClass({
    render: function() {
        return (
            <div>
                <h1>List</h1>
            </div>
        );
    }
});

module.exports = List;
